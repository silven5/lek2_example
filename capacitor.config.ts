import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.lek2_example',
  appName: 'lek2_example',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
